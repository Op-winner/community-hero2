# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional — override the model used for triage
GEMINI_MODEL=gemini-2.5-flash

# Passcode required to register an Admin (Mayor's Office) account.
# Change this to something private before sharing the app with anyone.
ADMIN_SIGNUP_CODE=MAYOR2026

# Supabase configuration for production-ready storage
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-service-role-or-anon-key

PORT=3000
